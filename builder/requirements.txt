# ---------- PyTorch CUDA 12.1 wheels ----------
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.1
torchaudio==2.4.1

# ---------- main libs ----------
whisperx
# pyannote.audio 4.x removed use_auth_token; 3.3+ requires speechbrain>=1.0
# whose .pretrained module was removed and from_hparams() changed.
# Pin to 3.1.1 (requires speechbrain>=0.5.14) + speechbrain 0.5.16.
# Refs: github.com/m-bain/whisperX/issues/1241, /issues/1406
pyannote.audio==3.1.1
speechbrain==0.5.16          # ECAPA embedding that whisperx uses
ctranslate2                  # used by whisperx

# ---------- speechbrain dependencies ----------
sentencepiece

# ---------- utilities ----------
pydub
# numpy 2.x removed np.NaN/np.Inf aliases that pyannote.audio 3.x still uses.
numpy<2.0
scipy
librosa
# huggingface_hub<0.30 keeps use_auth_token as a deprecated alias, which pyannote
# 3.x calls internally via hf_hub_download(use_auth_token=...).
huggingface_hub<0.30
runpod
cryptography
python-dotenv
